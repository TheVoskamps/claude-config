# Memory Index

- [This repo's executable code lives in two places](project_repo_executable_code_surfaces.md)
  — `.github/scripts/` is self-tested; `install.sh` and `plugins.sh`
  at the root are untested code, not prose.
