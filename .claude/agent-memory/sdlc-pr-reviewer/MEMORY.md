# Memory Index

- [Adjudicate count words before flagging](feedback_adjudicate_count_words_before_flagging.md)
  — classify each numeral as tally/constraint/anaphor/quotation/
  prose-continuation against `communication-style.md` → "No derivable
  numbers" before flagging; most grep hits are exempt.
- [Git command forms gated](project_git-command-forms-gated.md)
  — no `git -C`, no command substitution in git args, no `xargs` into
  git; bare literal git from cwd, and compare against `origin/main`.
